name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: fmt · clippy · test
    runs-on: ubuntu-latest
    env:
      # Trim debuginfo to line tables only: keeps backtrace line numbers while
      # cutting link-time memory, which was crashing ld (SIGBUS) when linking the
      # heavy moonlit-engine integration-test binaries. CI-scoped; local builds
      # keep full debuginfo.
      CARGO_PROFILE_DEV_DEBUG: line-tables-only
      CARGO_PROFILE_TEST_DEBUG: line-tables-only
    steps:
      - uses: actions/checkout@v4
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: rustfmt
        run: cargo fmt --all --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: test
        run: cargo test --all
      - name: git plugin (native, wasm-free)
        run: |
          cargo clippy --manifest-path plugins/git/Cargo.toml --all-targets -- -D warnings
          cargo test --manifest-path plugins/git/Cargo.toml
