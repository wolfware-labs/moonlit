[package]
name = "moonlit-plugin-github"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
license = "Elastic-2.0"
publish = false
description = "Moonlit first-party github plugin."

[lib]
name = "github"
crate-type = ["cdylib"]

[dependencies]
moonlit-sdk = { path = "../../sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[profile.release]
opt-level = "s"
